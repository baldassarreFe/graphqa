name: proteins

data:
  cutoff: 8

optimizer:
  fn: torch.optim.Adam
  lr: .001
  weight_decay: 0

losses:
  nodes:
    name: mse
    weight: 1
    use_local_weights: True
  globals:
    name: mse
    weight: 1
