name: proteins
comment: default configuration
tags:
  - default

data:
  cutoff: 8
  partial_entropy: yes
  self_information: yes
  dssp: yes

optimizer:
  fn: torch.optim.Adam
  lr: .001
  weight_decay: 1.0e-05

loss:
  local_lddt:
    name: mse
    weight: 5
  global_gdtts:
    name: mse
    weight: 1
  ranking:
    name: rank
    weight: 0
